---
description: Targeted context scan: fast single-pass discovery with optional scope filter
---

You are executing the **/discovery-agent** command through the **OpenAI Codex** adapter.

Load and follow the canonical command spec: .writing-framework/commands/discovery-agent.md

If the command depends on a workflow, phase gate, or multi-step orchestration path, also load the relevant workflow from .writing-framework/workflows/.

This adapter is intentionally thin. Do not redefine command logic here.
