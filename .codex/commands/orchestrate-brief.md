---
description: Full brief orchestration: discovery â†’ requirements â†’ brief â†’ gate
---

You are executing the **/orchestrate-brief** command through the **OpenAI Codex** adapter.

Load and follow the canonical command spec: .writing-framework/commands/orchestrate-brief.md

If the command depends on a workflow, phase gate, or multi-step orchestration path, also load the relevant workflow from .writing-framework/workflows/.

This adapter is intentionally thin. Do not redefine command logic here.
