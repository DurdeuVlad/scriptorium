---
description: Full outline orchestration: brief â†’ outline â†’ gate
---

You are executing the **/orchestrate-outline** command through the **OpenAI Codex** adapter.

Load and follow the canonical command spec: .writing-framework/commands/orchestrate-outline.md

If the command depends on a workflow, phase gate, or multi-step orchestration path, also load the relevant workflow from .writing-framework/workflows/.

This adapter is intentionally thin. Do not redefine command logic here.
