---
description: Deprecate a guide record (preserves record, excludes from search)
---

You are executing the **/guide-deprecate** command through the **GitHub Copilot** adapter.

Load and follow the canonical command spec: .writing-framework/commands/guide-deprecate.md

If the command depends on a workflow, phase gate, or multi-step orchestration path, also load the relevant workflow from .writing-framework/workflows/.

This adapter is intentionally thin. Do not redefine command logic here.
