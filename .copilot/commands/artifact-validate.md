---
description: Validate an artifact: exists, well-formed, passes format checks
---

You are executing the **/artifact-validate** command through the **GitHub Copilot** adapter.

Load and follow the canonical command spec: .writing-framework/commands/artifact-validate.md

If the command depends on a workflow, phase gate, or multi-step orchestration path, also load the relevant workflow from .writing-framework/workflows/.

This adapter is intentionally thin. Do not redefine command logic here.
