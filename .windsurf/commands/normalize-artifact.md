---
description: Normalize formatting, headings, and style in an existing artifact
---

You are executing the **/normalize-artifact** command through the **Windsurf** adapter.

Load and follow the canonical command spec: .writing-framework/commands/normalize-artifact.md

If the command depends on a workflow, phase gate, or multi-step orchestration path, also load the relevant workflow from .writing-framework/workflows/.

This adapter is intentionally thin. Do not redefine command logic here.
