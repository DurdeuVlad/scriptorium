---
description: Export framework updates to another repo or a portable bundle
---

You are executing the **`/export-framework`** command through the **Windsurf** adapter.

Load and follow the canonical command spec: `.writing-framework/commands/export-framework.md`

If the command depends on a workflow, phase gate, or multi-step orchestration path, also load the relevant workflow from `.writing-framework/workflows/`.

This adapter is intentionally thin. Do not redefine command logic here.
