## Summary

<!-- What changed and why (1–3 sentences) -->

## Area

- [ ] App (FastAPI / LangGraph / `frontend/`)
- [ ] Framework (`.writing-framework/`)
- [ ] MCP (`mcp/`)
- [ ] IDE adapters (`.claude/`, `.codex/`, etc.)
- [ ] Docs only

## Checklist

- [ ] No secrets (`.env`, API keys, Penpot tokens, `projects/`, `*.db`)
- [ ] Framework changes: noted eval run or reason skipped (`evals/cases/case-01`)
- [ ] Docs updated if behavior or setup changed
- [ ] `npm run lint` passes in `frontend/` (if UI touched)

## Test plan

<!-- Commands you ran, or "not run" with reason -->
